%li
  = image_tag 'link-icon.png'
  .links
    = link_to link_url(link.token), link_url(link.token), target: "_blank"
    %br
    %span= link_to truncate(link.url, length: 55), link.url, target: "_blank", title: link.url